<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="MMSEApp.Views.PatientsViewPage">
    <StackLayout>
        <SearchBar Placeholder="Search for Patients"/>
        <ListView x:Name="PatientsList">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell >
                        <StackLayout Orientation="Horizontal">
                            <Label x:Name="PatientID" Text="{Binding PatientID, StringFormat='Patient ID = {0}'}"/>
                            <Label x:Name="FirstName" Text="{Binding FirstName}" />
                            <Label x:Name="LastName" Text="{Binding LastName}"/>
                        
                        </StackLayout>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <Button Text="Add New Patient" Clicked="Add_New_Patient_Button_Clicked"/>
    </StackLayout>
</ContentPage>